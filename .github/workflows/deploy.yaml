name: 'Deploy App to S3'

#on: [push] # toutes les branches
# ou bien
#on:
  #push:
    #branches:
      #- main # uniquement sur la branche main

on:
  push:
    branches:
      - maBranch # uniquement sur la branche maBranch


jobs:
  deployment: # nom
    runs-on: ubuntu-latest # instance temporaire
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: List directory
      run: |
        ls -la

    - name: Create new file
      run: |
        touch newfile.txt